# Codex Agent 실행 지침

## 목적과 실행 위치
이 저장소는 설계 결과물이 아니라 **사용자의 로컬 CAD 작업 환경을 구축하는 실행 명세**이다.
README.md → docs/SETUP.md → docs/VALIDATION.md → docs/SOURCES.md 순서로 읽는다.
세부 명령은 설치된 버전의 `--help` 및 공식 문서와 대조한다. 문서의 기준일을 최신 버전 보장으로 해석하지 않는다.

작업 시작 시 실제 OS·CPU 아키텍처·현재 셸·작업 경로·로컬/WSL/컨테이너/클라우드 여부를 확인한다.
Windows용 설치 스크립트는 **실제 CAD를 사용할 Windows PC의 네이티브 PowerShell**에서만 실행한다.
클라우드 Codex 또는 Linux CI에서 저장소를 읽었다고 사용자의 Windows 프로그램에 접근할 수 있는 것은 아니다.
클라우드라면 문서/코드 검수만 하고 `BLOCKED_USER_ACTION: LOCAL_DESKTOP_REQUIRED`를 기록한다.
모델 식별자, 사용자 계정, CAD 설치 경로, 프린터 모델을 추측하지 않는다.

## 실행 순서
1. 요구사항·기존 설치·버전을 읽기 전용으로 조사한다. 설치된 CAD와 기존 프로젝트를 보존한다.
2. `scripts/bootstrap.ps1`을 **-Apply 없이** 실행해 설치 계획을 만든다. 기존 도구는 재사용한다.
3. 사용자가 환경 구축을 요청한 범위의 일반 설치를 진행하되, 인증·라이선스·관리자 권한·유료 구매·기존 프로그램 업그레이드에는 사용자 개입/승인 경계를 지킨다.
4. 수동 단계는 구체적인 화면/버튼과 이유를 안내한다. 불가능한 GUI 동작을 수행했다고 쓰지 않는다.
5. 실제 실행 검증을 진행하고 `templates/setup-report.md` 형식으로 `.local/setup-report.md`를 작성한다.
6. 각 단계의 상태, 실행 명령, 반환 코드, 버전, 증거 파일, 남은 수동 작업을 기록한다.

## 변경 제한
- 작업 폴더 외의 파일, 전역 CAD 설정, 사용자 라이브러리를 무단으로 덮어쓰지 않는다.
- 기존 3D 모델을 시험 데이터로 사용하지 않는다. 새 문서 또는 복제 프로젝트를 사용한다.
- 강제 종료, `git reset --hard`, force push, 영구 실행 정책 완화, 보안 기능 비활성화, 해시 검사 무시를 하지 않는다.
- 이미 설치된 도구를 자동 업그레이드/다운그레이드하지 않는다. 버전 충돌은 별도 계획으로 보고한다.
- 인증 토큰·API 키·Autodesk 계정·Codex 인증 파일을 읽어 수집하거나 Git에 넣지 않는다.
- MCP는 선택 사항이다. 임의 최신 버전 설치, 원격 코드 즉시 실행, 외부 네트워크 공개를 하지 않는다.
- Codex의 기존 설정은 전체 교체하지 않는다. 필요한 키만 검토 후 병합하고 백업 위치를 기록한다.
- Windows UAC 및 라이선스 동의를 자동 클릭하거나 자격증명을 채팅으로 요청하지 않는다.
- 설치 명령이 문서에 존재한다는 이유로 실행 결과를 성공으로 기록하지 않는다.

## 검증 원칙
상태는 `NOT_STARTED`, `RUNNING`, `VERIFIED`, `BLOCKED_USER_ACTION`, `FAILED`, `SKIPPED` 중 하나다.
`SKIPPED` 및 `BLOCKED_USER_ACTION`은 성공이 아니다. **정적 테스트·mock·파일 존재 확인만으로 CAD 실행을 검증했다고 판단하지 않는다.**
이 저장소 작성 시 수행한 테스트와 사용자 PC에서 지금 수행한 테스트를 분리한다.
실행 로그는 `.local/`에 저장하고 Git에 커밋하지 않는다. 결과를 공유할 때 개인 경로·계정명도 검토한다.
초기 범위는 Fusion 환경 설치와 시험 실행이다. 3D 프린터의 실제 출력 시작, 장비 제어, 유료 계약은 범위 밖이다.

## Fusion 추가 규칙
`adsk` 모듈은 Fusion 내부에서 실행한다. Design API의 cm/radian 단위를 처리한다. 원본 대신 새 시험 문서를 사용한다. 시험 형상 생성과 실제 3D 프린팅 적합성은 별개의 검증이다.
